- [SQLite migration rule](sqlite-migration-rule.md) — never add ALTER TABLE in sqliteMigrate() for columns already in CREATE TABLE; causes silent PDO errors every request
- [Admin sidebar CSS](admin-sidebar-css.md) — sidebar uses max-width transition on .sb-label, not display:none; mobile uses translateX
- [Client import/export columns](client-import-export.md) — export template must match exact column names the importer expects; see client-import.php header comment for canonical list
- [Pricing plans as shared source](pricing-plans-shared-source.md) — pricing_plans table is the single source for plan names; client-form package dropdown and subscription plan_name datalist both pull from it
- [DB migrations SQLite safety](db-migrations-sqlite.md) — db-migrations.php uses dbTableExists()/dbColumnExists() helpers that branch on DB_DRIVER; SHOW TABLES/SHOW COLUMNS are MySQL-only and must never be used directly
- [Stat card standard](stat-card-standard.md) — .admin-stat-card (admin.css) = dashboard summary cards (clickable, icon+value+label); .stat-card (theme.css) = inline metric boxes; they are DISTINCT components, not duplicates
- [Notifications schema canonical](notifications-schema.md) — canonical schema uses seen_at DATETIME (NULL = unread); is_read/read_at columns were old duplicate schema, removed from database.sql; portal/index.php and notify.php all use seen_at IS NULL
- [SQLite init unclosed exec string](sqlite-init-exec-string.md) — sqlite-init.php had an unclosed $pdo->exec() block; the fix requires "); before each function close; always run php -l after editing
- [M15 index schema mismatch](m15-index-schema.md) — M15 CREATE INDEX statements used wrong column names; tickets/client_subscriptions use user_id not client_id; users uses active not status
- [Dev environment bootstrap](dev-bootstrap.md) — dev-config.php only loads when APP_ENV=development env var is set; APP_SECRET_KEY must be inside dev-config.php; SUPERADMIN_EMAIL+SUPERADMIN_PASS_PLAIN must be Replit dev env vars
- [sqlite-init duplicate tables](sqlite-init-duplicates.md) — _sqliteInitSeedData() had 4 spurious CREATE TABLE blocks (amc_renewal_config, client_subscriptions, client_charge_history, branches) that were removed; canonical defs live in sqliteInit() and sqliteMigrate()
- [client-agreements parse error](client-agreements-parse.md) — had extra endif before endforeach in the agreements table loop (line ~450); removed the extra endif to fix Parse error: unexpected token "endif"
- [Legal pages CMS](legal-pages-cms.md) — privacy.php, terms.php, cookie-policy.php all read from site_settings: legal_privacy, legal_terms, legal_cookie; editable via admin/legal.php tabbed UI
- [Portal documents page](portal-documents.md) — portal/documents.php created; shows client_agreements and client_documents for the linked client_id; linked in portal-layout.php nav between invoices and faq
